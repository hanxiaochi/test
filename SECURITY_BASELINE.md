# 等保2.0二级技术基线说明

本项目以 `GB/T 22239-2019` 等保2.0二级常见技术控制作为产品安全基线。这里的“通过”只表示代码与部署配置自检通过，不代表已经完成定级、备案、测评、整改复测或取得测评机构结论。

## 已实现控制

- 身份鉴别：Scrypt 加盐摘要、至少 10 位且包含字母/数字/特殊字符、首次登录强制改密。
- 密码安全：默认 90 天有效期、最近 5 次密码禁止复用、管理员重置后撤销全部会话并强制用户再次修改。
- 登录防护：同一身份持久化限流；账号连续 5 次密码错误后锁定 30 分钟；管理员可以人工解锁。
- 会话控制：服务端仅保存令牌 SHA-256；默认闲置 30 分钟失效、绝对时长不超过 8 小时；改密、禁用、角色或项目变化立即撤销会话。
- 访问控制：租户、项目、角色和接口四层校验；内置管理员角色受保护；管理员可创建最小权限自定义角色并为用户分配多个角色。
- 安全审计：登录、改密、账号、角色、权限、项目、导入导出、备份和关键业务变更均留痕；默认基线要求至少保留 180 天。
- Web 防护：同源写请求校验、CSP、HSTS（仅 HTTPS）、防嵌入、MIME 嗅探保护、敏感响应禁止缓存、请求体与上传限制。
- 数据保护：SQLite 完整性与就绪检查、逐文件 SHA-256、附件一致性巡检、不可覆盖恢复和全系统备份验包。

## 自动检测

```bash
npm run verify:security-baseline
npm audit --omit=dev
npm run test:all
```

管理员在“账号权限管理”中可查看当前实例的等保技术基线报告。开发环境因未启用 HTTPS 会显示部署提示；正式环境必须设置 `NODE_ENV=production`、`APP_COOKIE_SECURE=true` 并由可信反向代理终止 TLS。

## 策略变量

```text
APP_PASSWORD_MAX_AGE_DAYS=90
APP_PASSWORD_HISTORY_COUNT=5
APP_ACCOUNT_LOCK_ATTEMPTS=5
APP_ACCOUNT_LOCK_MINUTES=30
APP_SESSION_IDLE_MINUTES=30
APP_SESSION_MAX_HOURS=8
APP_AUDIT_RETENTION_DAYS=180
```

程序只接受安全范围内的配置值，超出范围会回落到上述默认值。修改策略后必须重新执行完整测试，并由安全管理员复核风险。

## 正式等保工作仍需人工完成

正式测评还需要结合实际部署完成定级备案、网络边界与主机加固、恶意代码防范、日志集中留存与时间同步、漏洞扫描和渗透测试、备份恢复演练、制度与人员管理、供应链和机房条件等工作。这些内容不能由应用代码自动替代。
